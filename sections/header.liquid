<header style="position:sticky;top:0;background:#0b0b0b88;backdrop-filter:saturate(160%) blur(8px);border-bottom:1px solid #1f1f1f;z-index:10;">
  <div class="container" style="display:flex;align-items:center;justify-content:space-between;height:64px;">
    <div class="logo">
      <a href="{{ routes.root_url }}">
        {% assign logo_asset = 'logo.png' | asset_url %}
        <img src="{{ logo_asset }}" alt="{{ shop.name }}" style="height:34px;width:auto;">
      </a>
    </div>
    <nav style="display:flex;gap:1.25rem;align-items:center;">
      <a href="{{ routes.root_url }}">Accueil</a>
      <a href="{{ routes.all_products_collection_url }}">Shop</a>
      <a href="/pages/a-propos">À propos</a>
      <a href="/pages/contact">Contact</a>
      <a href="{{ routes.cart_url }}" aria-label="Panier" style="display:inline-flex;align-items:center;gap:.4rem;">
        🛒 <span class="muted">({{ cart.item_count }})</span>
      </a>
    </nav>
  </div>
</header>
{% schema %}
{
  "name": "Entête",
  "settings": []
}
{% endschema %}
